# Sensitive - NEVER commit
.env
*.key

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
*.egg-info/
dist/
build-python/
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/
.tox/
.python-version
.ipynb_checkpoints/

# Agent Smith runtime directories (created on-demand)
*.log
*_analysis.json
*_data.json
backups/
reports/
logs/
data/

# Marketplace distribution - scripts are maintained in main scripts/ directory
# and synced to skill location, so we gitignore the copy
agent-smith-plugin/skills/agent-smith/scripts/

# Onboarding state (tracked as template, but user's state will be overwritten)
# Note: data/onboarding_state.json is committed as a template, but the user's
# actual state will modify this file during onboarding. This is intentional.

# Build reference (temporary - remove before publishing)
build/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files
*.tmp
*.temp
*.bak
